#include <iostream>
#include <queue>

using namespace std;

/*
Priority queue is a queue whos front element always contains
the max element (max heap) or the min element (min heap)
*/

int PriorityQueue() {

  // Initialize
  priority_queue<int> pq;

  return 0;
}